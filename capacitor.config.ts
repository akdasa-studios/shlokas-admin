import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.akdasa.app',
  appName: 'Application',
  webDir: 'dist',
  bundledWebRuntime: false
}

export default config
